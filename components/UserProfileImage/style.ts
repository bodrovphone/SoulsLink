import {StyleSheet} from 'react-native';

const style = StyleSheet.create({
  userImageContainer: {
    borderColor: '#F35BAC',
    borderWidth: 1,
    borderRadius: 50,
    padding: 4,
  },
  image: {
    width: 65,
    height: 65,
  },
});

export default style;
